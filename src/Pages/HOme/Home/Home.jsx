import React from 'react';
import Banner from './Banner/Banner';
import Uses from '../../Uses/Uses';

const Home = () => {
    return (
        <div>
            <Banner></Banner>
            <Uses></Uses>
        </div>
    );
};

export default Home;